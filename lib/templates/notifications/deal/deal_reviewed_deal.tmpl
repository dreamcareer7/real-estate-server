{% if data.Approved.length > 0 %}{{data.Approved.length}} Approved:
  {% for approved in data.Approved %}
    {{approved}}
  {% endfor %}
{% endif %}

{% if data.Declined.length > 0 %}{{data.Declined.length}} Declined:
  {% for declined in data.Declined %}
    {{declined}}
  {% endfor %}
{% endif %}
