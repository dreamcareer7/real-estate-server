{% set title = room.title or room.proposed_title %}
{% if title and transport != "SMS" %}[{{title}}] {% endif %}{{subjects[0] | name}}: {{objects[0].comment}}
{% if transport == "SMS" %} {{branch}}.{% endif %}
