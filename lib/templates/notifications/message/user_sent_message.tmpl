{% set title = room.title or room.proposed_title %}
{% if room.room_type != "Direct" %}{{subjects[0] | name}} :111 {% endif %}{{objects[0].comment | safe}}
{% if transport == "SMS" %} {{branch}}.{% endif %}
