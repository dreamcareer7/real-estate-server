{% set title = room.title or room.proposed_title %}
{% if (not before) or (before == 0) %}
[New Price] {% if title %}{{title}}: {% endif %}{{listing | listing_address}} is now {{now | price}}.
{% else %}
[Price Change] {% if title %}{{title}}: {% endif %}{{listing | listing_address}} went from {{before | price}} to {{now | price}}.
{% endif %}
