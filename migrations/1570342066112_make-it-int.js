const db = require('../lib/utils/db')

const migrations = [
  'ALTER TABLE envelopes_documents ALTER document_id TYPE int'
]


const run = async () => {
  const { conn } = await db.conn.promise()

  for(const sql of migrations) {
    await conn.query(sql)
  }

  conn.release()
}

exports.up = cb => {
  run().then(cb).catch(cb)
}

exports.down = () => {}
