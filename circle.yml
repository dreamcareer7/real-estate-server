machine:
  node:
    version: 6.3.0
  services:
    - redis
dependencies:
  override:
    - npm install --no-progress --dev
    - npm install istanbul
test:
  override:
    - node_modules/istanbul/lib/cli.js cover --dir $CIRCLE_ARTIFACTS tests/run.js -- --curl --stop-on-fail